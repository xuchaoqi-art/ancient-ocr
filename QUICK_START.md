# 🚀 快速开始指南

## 一键部署

### 方案A：使用Render Blueprint（推荐）

点击下面的按钮直接部署：

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/kimi-ocr/ancient-ocr)

**部署步骤：**
1. 点击上面的按钮
2. 登录/注册Render账户
3. 连接GitHub（如果需要）
4. 点击"Approve and Deploy"
5. 等待5-10分钟完成部署
6. 获得访问URL

### 方案B：手动部署到Render

如果您想使用自己的GitHub账户部署：

1. **Fork仓库**
   - 访问 https://github.com/kimi-ocr/ancient-ocr
   - 点击"Fork"按钮

2. **一键部署**
   - 点击您Fork后的仓库中的"Deploy to Render"按钮
   - 或在Render控制台手动创建服务

### 方案C：本地测试

```bash
# 1. 克隆仓库
git clone https://github.com/kimi-ocr/ancient-ocr.git
cd ancient-ocr

# 2. 安装依赖
pip install -r requirements.txt

# 3. 运行应用
python app.py

# 4. 访问 http://localhost:5000
```

## 📋 部署验证清单

部署完成后，请验证以下功能：

### ✅ 基础功能
- [ ] 打开网站首页
- [ ] 上传文件功能正常
- [ ] 进度条显示正常
- [ ] 状态更新正常

### ✅ OCR功能
- [ ] 上传测试PDF（提供测试文件）
- [ ] OCR识别完成
- [ ] 结果预览正常
- [ ] 下载功能正常

### ✅ 性能测试
- [ ] 上传50页PDF
- [ ] 5分钟内完成处理
- [ ] 输出文件完整

## 📁 测试文件

### 测试用PDF

创建测试PDF文件：

```python
# test_pdf.py
from reportlab.pdfgen import canvas
from reportlab.lib.pagesizes import A4
import os

def create_test_pdf(filename, pages=10):
    c = canvas.Canvas(filename, pagesize=A4)
    width, height = A4
    
    for i in range(pages):
        # 添加竖排文字模拟（实际为横排，仅作测试用）
        text = f"第 {i+1} 页\n\n"
        text += "天地玄黄，宇宙洪荒。\n"
        text += "日月盈昃，辰宿列张。\n"
        text += "寒来暑往，秋收冬藏。\n"
        text += "闰余成岁，律吕调阳。\n"
        
        c.drawString(100, height - 100, text)
        c.showPage()
    
    c.save()
    print(f"创建测试PDF: {filename}, {pages}页")

# 创建测试文件
if __name__ == "__main__":
    create_test_pdf("test_10pages.pdf", 10)
    create_test_pdf("test_50pages.pdf", 50)
```

### 测试图片

创建测试图片：

```python
# test_image.py
from PIL import Image, ImageDraw, ImageFont
import os

def create_test_image(filename, text="测试文字"):
    # 创建白色背景
    img = Image.new('RGB', (800, 1000), color='white')
    draw = ImageDraw.Draw(img)
    
    # 添加文字
    try:
        font = ImageFont.truetype("/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf", 24)
    except:
        font = ImageFont.load_default()
    
    draw.text((50, 50), text, fill='black', font=font)
    
    img.save(filename)
    print(f"创建测试图片: {filename}")

if __name__ == "__main__":
    create_test_image("test_image.png", "天地玄黄\n宇宙洪荒\n日月盈昃\n辰宿列张")
```

## 🔧 配置说明

### 环境变量

| 变量名 | 说明 | 默认值 | 备注 |
|--------|------|--------|------|
| PORT | 服务端口 | 5000 | Render自动注入 |
| MAX_WORKERS | 工作进程数 | 2 | 根据实例调整 |
| TIMEOUT | 超时时间 | 300 | 秒 |

### 资源限制

**免费版限制：**
- 内存: 512MB
- CPU: 0.1 vCPU
- 磁盘: 2GB
- 月度使用时间: 500小时

**推荐配置：**
- 内存: 1GB+
- CPU: 0.5 vCPU+
- 磁盘: 5GB+

## 📊 监控和日志

### Render控制台

访问Render控制台查看：
- 部署日志
- 运行日志
- 性能监控
- 资源使用情况

### 健康检查

健康检查端点：`GET /`

预期响应：
```
200 OK
```

## 🐛 常见问题

### 1. 部署失败

**问题：** 构建失败或启动失败

**解决：**
- 检查requirements.txt格式
- 查看部署日志
- 确保app.py可以正常运行

### 2. 模型下载超时

**问题：** 首次部署时PaddleOCR模型下载失败

**解决：**
- 重新部署
- 或使用预构建镜像

### 3. 内存不足

**问题：** 处理大文件时内存溢出

**解决：**
- 升级到付费实例
- 限制上传文件大小
- 优化处理流程

### 4. 识别准确率低

**问题：** OCR识别结果不准确

**解决：**
- 确保图片清晰度
- 调整图片分辨率
- 使用图像预处理

## 📞 技术支持

### 获取帮助

1. **查看日志**
   - Render控制台日志
   - 应用日志文件

2. **社区支持**
   - GitHub Issues
   - 技术论坛

3. **官方文档**
   - [PaddleOCR文档](https://paddlepaddle.github.io/PaddleOCR/)
   - [Flask文档](https://flask.palletsprojects.com/)
   - [Render文档](https://render.com/docs)

## 🎉 成功部署标志

当您看到以下情况时，说明部署成功：

1. ✅ Render控制台显示"Live"
2. ✅ 网站可以正常访问
3. ✅ 可以上传文件
4. ✅ OCR处理正常
5. ✅ 可以下载结果

## 下一步

部署成功后，您可以：

1. **自定义域名**
   - 在Render添加自定义域名
   - 配置DNS解析

2. **性能优化**
   - 升级到更高配置
   - 添加缓存机制

3. **功能扩展**
   - 添加用户管理
   - 增加批量处理

4. **监控告警**
   - 设置性能监控
   - 配置错误告警

## 快速链接

- **GitHub仓库**: https://github.com/kimi-ocr/ancient-ocr
- **Render部署**: https://render.com/deploy?repo=https://github.com/kimi-ocr/ancient-ocr
- **在线演示**: https://ancient-ocr.onrender.com (示例)

---

**祝您部署顺利！如有问题，请随时联系。** 🚀