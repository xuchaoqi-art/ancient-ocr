# 古籍OCR + 竖排转横排

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/kimi-ocr/ancient-ocr)

> **🚀 一键部署**: 点击上方按钮，5分钟完成部署，立即使用！

一个基于PaddleOCR的古籍OCR在线工具，能够将古籍竖排文字准确转换为现代横排格式。

## ✨ 功能特色

- **📄 多格式支持**：PDF、JPG、PNG、GIF、BMP、ZIP压缩包
- **🧠 智能识别**：基于PaddleOCR深度学习模型，准确识别竖排古籍文字
- **🔄 竖排转横排**：按照古籍阅读顺序（右→左，上→下）自动转换为现代横排
- **📊 实时进度**：上传和处理进度实时显示
- **👁️ 在线预览**：原图和识别结果在线预览
- **📥 多种输出**：生成单页文本、完整文本、Word文档

## 🚀 快速开始

### 在线部署

点击上方的 "Deploy to Render" 按钮，一键部署到Render平台：

1. 点击按钮跳转到Render部署页面
2. 连接您的GitHub账户
3. 配置环境变量（仅PORT，平台自动注入）
4. 点击 "Deploy" 开始部署
5. 等待部署完成，即可获得在线访问地址

### 本地开发

#### 环境要求

- Python 3.11+
- 4GB+ RAM（推荐8GB）
- 2GB+ 磁盘空间

#### 安装步骤

1. 克隆仓库
```bash
git clone https://github.com/yourusername/ancient-ocr.git
cd ancient-ocr
```

2. 创建虚拟环境
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
```

3. 安装依赖
```bash
pip install -r requirements.txt
```

4. 下载PaddleOCR模型（首次运行自动下载）
```python
from paddleocr import PaddleOCR
ocr = PaddleOCR(use_angle_cls=True, lang='ch')
```

5. 运行应用
```bash
python app.py
```

6. 访问 http://localhost:5000

### Docker部署

#### 构建镜像

```bash
docker build -t ancient-ocr .
```

#### 运行容器

```bash
docker run -d \
  --name ancient-ocr \
  -p 5000:5000 \
  -v $(pwd)/uploads:/app/uploads \
  -v $(pwd)/output:/app/output \
  ancient-ocr
```

#### 使用Docker Compose

```yaml
version: '3.8'
services:
  ancient-ocr:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./output:/app/output
    environment:
      - PORT=5000
    restart: unless-stopped
```

## 📖 使用说明

### 1. 上传文件
- 拖拽文件到上传区域，或点击选择文件
- 支持格式：PDF、JPG、JPEG、PNG、GIF、BMP、ZIP
- 单文件最大50MB

### 2. 等待处理
- 系统自动进行OCR识别和竖排转横排处理
- 实时显示处理进度和状态

### 3. 查看结果
- 在线预览识别结果
- 查看处理统计信息

### 4. 下载文件
- 点击"下载全部结果"按钮
- 获得包含以下文件的zip压缩包：
  - `single_page_xxx_hengpai.txt` - 每页单独的文本文件
  - `full_book.txt` - 完整文本文件
  - `full_book.docx` - Word文档（含段落格式）

## 🔧 API接口

### 上传文件
```http
POST /upload
Content-Type: multipart/form-data

file: <binary_data>
```

响应：
```json
{
  "task_id": "uuid-string",
  "status_url": "https://.../status/uuid-string"
}
```

### 查询状态
```http
GET /status/{task_id}
```

响应：
```json
{
  "status": "processing|completed|failed",
  "progress": 75,
  "message": "OCR识别第 3/4 页...",
  "result_url": "https://.../download/task_id"
}
```

### 下载结果
```http
GET /download/{task_id}
```

## 🛠️ 技术架构

### 前端
- HTML5 + CSS3 + JavaScript
- Tailwind CSS（样式框架）
- Anime.js（动画库）
- ECharts.js（数据可视化）
- Splide.js（轮播组件）

### 后端
- Python 3.11
- Flask 2.x（Web框架）
- PaddleOCR（OCR引擎）
- python-docx（Word文档生成）
- pdf2image（PDF转图片）

### 部署
- Docker容器化
- gunicorn（WSGI服务器）
- Render云平台

## 📊 性能指标

| 指标 | 数值 |
|------|------|
| 单页处理时间 | 1-3秒（取决于图片复杂度） |
| 支持最大文件 | 50MB |
| 支持最大页数 | 100页（推荐） |
| 识别准确率 | 85%+（古籍竖排文字） |

## 🎨 设计特色

- **传统文化风格**：米白色背景，墨绿色主色调，体现古籍韵味
- **现代交互体验**：流畅动画，实时反馈，响应式设计
- **简洁易用**：拖拽上传，一键处理，无需专业知识

## 🔍 竖排转横排算法

本项目采用专有的竖排文字排序算法：

1. **文字检测**：使用PaddleOCR检测文字框坐标
2. **方向判断**：分析文字框的排列方向
3. **排序规则**：按照"右→左，上→下"的古籍阅读顺序排序
4. **段落合并**：智能合并相邻文字块，形成完整段落
5. **格式输出**：生成现代横排格式的文本

## 🐛 常见问题

### Q: 识别准确率不高怎么办？
A: 可以尝试以下方法：
- 确保图片清晰，分辨率200DPI以上
- 避免图片倾斜或变形
- 对于特别模糊的古籍，建议先进行图像增强

### Q: 处理速度慢怎么办？
A: 建议：
- 减少单次上传的页数
- 使用较小的图片尺寸（但保持清晰度）
- 升级到更高配置的服务器

### Q: 支持哪些语言？
A: 主要针对中文古籍优化，也支持英文和数字识别。

### Q: 如何部署到自己的服务器？
A: 参考"Docker部署"部分，或使用Render一键部署。

## 🤝 贡献指南

欢迎提交Issue和Pull Request！

### 开发环境设置

1. Fork本仓库
2. 创建特性分支：`git checkout -b feature/your-feature`
3. 提交更改：`git commit -am 'Add some feature'`
4. 推送到分支：`git push origin feature/your-feature`
5. 创建Pull Request

### 代码规范

- 使用Black进行代码格式化
- 遵循PEP 8规范
- 添加必要的注释和文档

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- [PaddleOCR](https://github.com/PaddlePaddle/PaddleOCR) - 强大的OCR引擎
- [Tailwind CSS](https://tailwindcss.com/) - 优秀的CSS框架
- [Render](https://render.com/) - 便捷的云平台

## 📞 联系方式

- 项目地址：[https://github.com/yourusername/ancient-ocr](https://github.com/yourusername/ancient-ocr)
- Issue反馈：[https://github.com/yourusername/ancient-ocr/issues](https://github.com/yourusername/ancient-ocr/issues)

---

**传承文化，智能识别** - 让古籍数字化变得简单